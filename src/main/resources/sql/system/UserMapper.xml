<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jm.springboottemplate.system.dao.UserDao">
    <select id="getUserByUsername" resultType="com.jm.springboottemplate.system.domain.User">
        SELECT u.user_id AS userId,
               u.username,
               u.password,
               u.enabled,
               u.locked
        FROM user u
        WHERE username = #{username}
        <![CDATA[
        /*project_name:SpringBootTemplate,namespace:com.jm.springboottemplate.system.dao.UserDao,sqlid:getUserByUsername*/
        ]]>
    </select>

    <select id="getUserRolesByUserId" resultType="com.jm.springboottemplate.system.domain.Role">
        SELECT r.role_id AS roleId,
               r.name,
               r.name_zh AS nameZh
        FROM user_role ur
                 LEFT JOIN role r ON ur.role_id = r.role_id
        WHERE ur.user_id = #{userId}
        <![CDATA[
        /*project_name:SpringBootTemplate,namespace:com.jm.springboottemplate.system.dao.UserDao,sqlid:getUserRolesByUserId*/
        ]]>
    </select>
</mapper>