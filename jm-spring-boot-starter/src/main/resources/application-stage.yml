server:
  port: 8080

spring:
  cache:
    cache-names: c1,c2
    redis:
      time-to-live: 1800s
  datasource:
    name: datasource-one
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://127.0.0.1:3306/spring_boot_template?useUnicode=true&characterEncoding=utf8
    username: spring_boot_template_w
    password: H'Tkh9@!*_/B'_j
  devtools:
    restart:
      enabled: false
  redis:
    database: 0
    host: localhost
    port: 6379
    password: 123456
    timeout: 10000ms
    lettuce:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0

custom:
  configuration:
    ignores:
      # POST Requests need to be ignored.
      post:
        - "/auth/register"
        - "/auth/login"
      get:
        - "/auth/checkUsernameUniqueness"
        - "/auth/checkEmailUniqueness"
        - "/auth/validateUsername/**"
      # Request need to be ignored that matches pattern.
      pattern:
